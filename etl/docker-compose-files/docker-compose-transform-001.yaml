# BASE
version: '3.3'
services:
  datasusftp:
    container_name: datasus-etl
    volumes:
      - '../scripts:/app/scripts'
      - '../data:/data'
    environment:
      - SCRIPT_NAME=transform_base.py
    image: gleytonlima/datasusftp